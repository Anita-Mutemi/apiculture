// this is to add the toggle effect to the slide-content class
function toggleSlide(element) {
    var slideContent = element.querySelector('.slide-content');
    slideContent.classList.toggle('active');
}
